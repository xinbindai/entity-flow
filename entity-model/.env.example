# Copy to .env and fill in. .env is gitignored -- never commit real credentials.
#
# Postgres connection used by test/testdata.py. Needs a database you are happy
# for the tests to create and drop a `poll_test` schema in; nothing outside
# that schema is touched.
POSTGRES_URL=postgresql://user:password@127.0.0.1/testdb
